# Versiyalar kilidlidir. Səbəb: versiyasız yazılanda `pip install -r` serverdə
# həmin günün ən yeni paketlərini çəkir — testlərin keçdiyi versiyaları yox.
# LangChain və pydantic bu layihədə ən tez dəyişənlərdir.
#
# Buradakı dəyərlər 2026-07-31-də 195 testin keçdiyi versiyalardır.
# Yeniləmək: paketi əl ilə qaldır → `pytest` → keçirsə bu faylı yenilə.
#
# Docker image üçün transitiv asılılıqlar da kilidlənməlidir:
#     pip freeze > requirements.lock.txt

# --- Veb çərçivə ---
fastapi==0.140.8
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
sse-starlette==3.4.6

# --- Verilənlər bazası ---
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
pgvector==0.5.0
alembic==1.18.5

# --- Keş ---
redis==8.0.1

# --- LangChain / LLM ---
langchain==1.3.14
langchain-core==1.5.1
langchain-anthropic==1.5.2
langchain-google-genai==4.3.2
langchain-ollama==1.1.0

# --- Analiz / toplama ---
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==6.1.1
dnspython==2.8.0
python-whois==0.9.6
tldextract==5.3.1

# --- Hesabat ---
reportlab==5.0.0

# --- MCP ---
mcp==1.28.1

# --- Testlər ---
pytest==9.1.1
pytest-asyncio==1.4.0
respx==0.23.1
